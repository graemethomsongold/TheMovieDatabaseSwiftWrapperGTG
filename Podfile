# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TMDBSwift-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TMDBSwift-iOS
  pod 'PromiseKit', '~> 6.8'

  target 'TMDBSwift-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TMDBSwift-macOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TMDBSwift-macOS
  pod 'PromiseKit', '~> 6.8'

  target 'TMDBSwift-macOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TMDBSwift-tvOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TMDBSwift-tvOS
  pod 'PromiseKit', '~> 6.8'

  target 'TMDBSwift-tvOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
